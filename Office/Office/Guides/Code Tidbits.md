
** Qualified and Not Qualified Popup **
Script name for popup `LiveOpsNewsDataService.cs`

**`OnBoardingFeedDataBuilder.cs`**
- static class
- check for level

`UserState` -> Tutorial 
`Career ` -> Configs -> LevelTable

`GameOnBoarding.cs`
check pending task to be executed when come home.

Pro tour Level
`_proTourUnlockLevel = SocialConfigs.UserProgressObjectAdapter.ProTourUnlockLevel;`

### Run Specific course with hole
In `MatchDataBuilder.cs` search for holeInfo and change the variables to hardcoded values of course and hole e.g. ` HoleInfo = CourseInfoProvider.GetHoleInfo(1, 12),`